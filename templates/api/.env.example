# === App ===
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# === Database ===
# Postgres connection string. Used by the app (src/config/env.ts) and by the
# migration runner (scripts/db-migrate.sh). Host-agnostic by design: any
# Postgres provider works.
DATABASE_URL=
