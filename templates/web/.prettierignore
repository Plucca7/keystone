# Generated output and dependencies — never formatted.
node_modules/
dist/
.next/
coverage/

# Lockfile is machine-generated in the package manager's own format, not Prettier's.
pnpm-lock.yaml

# Agent harness (Layer B) — the coding agent's own configuration and working notes,
# laid on top of the project: agent config, specs, hand-off briefings, daily logs, and
# long-term memory. None of it is product source — it is living process text written by
# people and the agent — so the product's code formatter does not govern it.
.claude/
specs/
memory/
knowledge/
docs/agent-harness.md
keystone.json
